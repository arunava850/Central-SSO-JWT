# Central Auth (get these from Central Auth team)
CENTRAL_AUTH_BASE_URL=https://auth.yourdomain.com
CLIENT_ID=my-spoke-app
REDIRECT_URI=http://localhost:3001/auth/callback
APP_SLUG=A1003

# Optional: JWT issuer (if different from Central Auth default)
# JWT_ISSUER=ains-auth-service

# Session secret (change in production)
SESSION_SECRET=change-me-in-production

# Port for this sample spoke app
PORT=3001
