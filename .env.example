# Microsoft Entra ID Configuration
TENANT_ID=your-tenant-id-here
# For CIAM (Customer Identity and Access Management), set tenant name:
# ENTRA_TENANT_NAME=yourtenant
# If ENTRA_TENANT_NAME is set, authority will be: https://{tenant-name}.ciamlogin.com/{tenant-id}
# If not set, uses standard: https://login.microsoftonline.com/{tenant-id}
CLIENT_ID=your-client-id-here
CLIENT_SECRET=your-client-secret-here

# Server Configuration
PORT=3000
BASE_URL=https://your-domain.com
HTTPS_ENABLED=true
NODE_ENV=production

# Redirect URIs (comma-separated)
# These must match the redirect URIs configured in your Entra ID app registration
REDIRECT_URIS=https://spoke-app1.com/auth/callback,https://spoke-app2.com/auth/callback,http://localhost:3001/auth/callback

# Allowed CORS Origins (comma-separated)
ALLOWED_ORIGINS=https://spoke-app1.com,https://spoke-app2.com,http://localhost:3001

# JWT Configuration
# Option 1: Use file paths
JWT_PRIVATE_KEY_PATH=./keys/private.pem
JWT_PUBLIC_KEY_PATH=./keys/public.pem

# Option 2: Use environment variables (PEM format with \n for newlines)
# JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----
# JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...\n-----END PUBLIC KEY-----

# JWT Expiration (in minutes)
JWT_EXPIRATION_MINUTES=15

# Google OAuth Configuration (optional - for multi-provider support)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# SSL Certificate Configuration (for HTTPS)
# SSL_KEY_PATH=./certs/key.pem
# SSL_CERT_PATH=./certs/cert.pem
